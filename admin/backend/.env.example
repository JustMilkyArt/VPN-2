SECRET_KEY=your-secret-key-change-in-production-min-32-chars
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=1440
DATABASE_URL=sqlite:///./vpn_admin.db
ADMIN_USERNAME=admin
ADMIN_PASSWORD=changeme123
BACKEND_HOST=0.0.0.0
BACKEND_PORT=8000
CORS_ORIGINS=["http://localhost:3000","http://localhost:8080","http://localhost"]
# API key for the Windows VPN client app (baked into the Flutter binary at build time)
# Generate with: python3 -c "import secrets; print(secrets.token_urlsafe(32))"
CLIENT_API_KEY=change-this-to-a-random-secret-key
